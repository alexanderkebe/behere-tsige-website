# Copy to .env.local and fill in. On Vercel, set these in the dashboard.

# Supabase — browser-safe (publishable / anon key)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Supabase — server-only SECRET key (never expose to the browser)
SUPABASE_SECRET_KEY=
